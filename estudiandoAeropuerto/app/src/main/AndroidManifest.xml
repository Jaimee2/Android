<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.estudiandoaeropuerto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EstudiandoAeropuerto"
        android:name=".Aeropuertos">
        <activity android:name=".HorariosSalidaActivity" />
        <activity android:name=".NuevoHorarioSalida" />
        <activity android:name=".DestinosActivity" />
        <activity android:name=".NuevoAeropuertoActivity" />
        <activity android:name=".NuevoDestinoActivity" />
        <activity
            android:name=".MainAeropuertoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>